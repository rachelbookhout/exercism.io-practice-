class ETL
  def transform(old)
    new_hash = {}

    #value return is sorted by abcs
    #if the hashes were different intially, only sorted within the indv. hashes
    #the hash is reversed (a => 1)
    #letter is downcased
    #%w(foo bar) = ["foo", "bar"]
  end
end
